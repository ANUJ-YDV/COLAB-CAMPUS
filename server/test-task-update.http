### Test Task Update Route (PUT method for drag-and-drop)

# First, login to get a token
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "test@collabcampus.com",
  "password": "hashedPlaceholder"
}

###

# Update task status using PUT /:id (for drag-and-drop)
# Replace <TASK_ID> with an actual task ID from your database
# Replace <YOUR_TOKEN> with the token from login
PUT http://localhost:5000/api/tasks/<TASK_ID>
Content-Type: application/json
Authorization: Bearer <YOUR_TOKEN>

{
  "status": "in-progress"
}

###

# Alternative: Update task status using PATCH /:taskId/status (existing route)
PATCH http://localhost:5000/api/tasks/<TASK_ID>/status
Content-Type: application/json
Authorization: Bearer <YOUR_TOKEN>

{
  "status": "done"
}

###

# Get all tasks for a project to find task IDs
GET http://localhost:5000/api/tasks/project/<PROJECT_ID>
Authorization: Bearer <YOUR_TOKEN>
